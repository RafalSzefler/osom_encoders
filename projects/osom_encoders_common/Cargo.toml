[package]
name = "osom_encoders_common"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
rust-version.workspace = true

[lib]
crate-type = ["lib"]

[dependencies]
const_format = { workspace = true }

[dev-dependencies]
rstest = { workspace = true }
